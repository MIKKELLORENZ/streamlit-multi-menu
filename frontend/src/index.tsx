import React from "react"
import ReactDOM from "react-dom"
import StreamlitMultiMenu from "./streamlit_multi_menu"

ReactDOM.render(
  <React.StrictMode>
    <StreamlitMultiMenu />
  </React.StrictMode>,
  document.getElementById("root")
)
